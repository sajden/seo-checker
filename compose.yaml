services:
  seo-monitor:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3010:3010"
    env_file:
      - .env.local
    environment:
      DATA_DIR: /data
      PORT: 3010
    volumes:
      - ${DATA_HOST_DIR:-./docker-data}:/data
    restart: unless-stopped
